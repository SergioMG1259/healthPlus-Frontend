import { IssueFormatPipe } from './issue-format.pipe';

describe('IssueFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new IssueFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
